Clone - git clone http.......
git status - to get the status of repository
git add . - to move changes from unstage to stage
git commit -m "message" --- to commit changes to git
git push origin main --- to upload local repo content to remote repo


mkDir - To create folder
init command - 
git remote add origin <-link->
git remote -v ---(to verify remo)
git branch ---(to check the branch)
git branch -M <name> (to rename branch)
git push  -u origin main(-u will be use so that next time you push you only have to use git push)

** Branch Commands **
git branch 
git branch -M <name> (to rename the branch)
git checkout <-branch name-> (to navigate)
git checkout -b <-new branch name - > (to create branch)
git branch -d <branch-name> -- to delete branch

**git branch command end


**merging code

# First way 
git diff <branchname> (to compare commits,branches,files & more)
git merge <branchname> (to merge 2 branches)

# Second Way
pull request
-- It lets you tell others about changes you've pushed to a branch in a repository on Github